name: doc-sync

on:
  pull_request:
    paths:
      - JA.md
      - EN.md
      - scripts/check_doc_sync.cr
      - .github/workflows/doc-sync.yml
      - README.md
  push:
    branches:
      - main
    paths:
      - JA.md
      - EN.md
      - scripts/check_doc_sync.cr
      - .github/workflows/doc-sync.yml
      - README.md
  workflow_dispatch:

jobs:
  sync-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v6
      - uses: crystal-lang/install-crystal@v1
      - name: Verify JA/EN structure sync
        run: crystal run scripts/check_doc_sync.cr -- JA.md EN.md
